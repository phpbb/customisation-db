### MySQL

* Click on the `Terminal` tab at the bottom of VS Code and make sure the `bash` window is selected in the right sidebar.
* Run `mysql -h 127.0.0.1 -u phpbb -pphpbb phpbb` to log into the MySQL database (this automatically passes the password of `phpbb` and the database name of `phpbb`).