phpbb.titania.contribution:
    resource: "routes/contribution.yml"
    prefix: /customise/db/{contrib_type}/{contrib}
    requirements:
        contrib_type: mod|extension|style|converter|official_tool|bbcode|bridge|translation

phpbb.titania.manage:
    resource: "routes/manage.yml"
    prefix: /customise/db/manage

phpbb.titania.general:
    resource: "routes/general.yml"
    prefix: /customise/db
