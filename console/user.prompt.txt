Perform a complete validation of all files in the vector store (using the manifest to understand how the file and directory hierarchy works) using the instructions in the system prompt. Exclude listing any files that had no issues. Produce the output as specified in the instructions. At the end of the report, based on the validation report provide a conclusion as to whether it should be approved or denied with a percentage confidence level in your assessment so a human can quickly make a determination. Return the response in json formatting with four field: the extension name ("name"), the validation report ("report"), the recommended outcome ("outcome") and the confidence ("confidence") percentage level in the recommended outcome.